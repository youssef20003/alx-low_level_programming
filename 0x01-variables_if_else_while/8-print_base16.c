#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - entry point
 *disctiptin : 'checks if number is + or -'
 *Return: always 0
  */
int main(void)
{
for(int i = 48; i <= 57; i++)
{
putchar(i);  
}
for(int i = 'a'; i <= 'f'; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
