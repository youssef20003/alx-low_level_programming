#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - entry point
 *disctiptin : 'checks if number is + or -'
 *Return: always 0
  */
int main(void)
{
int n = 48;
int a = 'a';
while (n <= 56)
{
putchar(n);
n++;  
}
while (a <= 'f')
{
putchar(a);
a++;
}
putchar('\n');
return (0);
}
